[project]
name = "bankanal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "coronaforhub@gmail.com", email = "sesivere@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "discord>=2.3.2",
    "dotenv>=0.9.9",
    "logging>=0.4.9.6",
]

[project.scripts]
bankanal = "bankanal:run"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
